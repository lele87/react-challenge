import styled from 'styled-components';

const StyledButton = styled.button`
	border-radius: 10px;
	font-size: 24px;
	width: 120px;
	height: 40px;
	margin-top: 10px;
	cursor: pointer;
	font-family: 'Ubuntu';
`;

export default StyledButton;
